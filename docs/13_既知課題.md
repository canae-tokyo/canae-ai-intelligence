# 既知課題（Known Issues）

## Design Principles

1. ブランド・URL・GitHub・インフラは独立した4レイヤーとして管理する。
2. AI IntelligenceはWeb Assistの研究・検証部門として位置付けるが、システムは完全に独立させる。
3. 公開ベンチマークとCANAE独自評価を混同しない。
4. AIニュースサイトではなく、AI業界を構造化・可視化する知識基盤を目指す。
5. ニュースよりもCANAEの実務検証を価値の中心に据える。

## 位置付け

この文書は、不具合一覧ではありません。v1.0.1相当の本番運用開始時点で、意図的に未実装または今後対応予定として残している項目を管理します。

## Version

| 項目 | 内容 |
| --- | --- |
| 対象版 | v1.0.1相当 |
| 状態 | Production 安定運用 / Freeze |
| 次回対応予定 | v1.1 |

## 現在の課題

| 区分 | 課題 | 影響 | 対応予定 |
| --- | --- | --- | --- |
| データ | `tools.json`は主要24件を初期実データ化済み、未確認22件は隔離済み | 実務利用前に残データの確認が必要 | v1.1 |
| データ | `news.json`は公式一次情報18件を初期投入済み | ニュース品質は継続拡充が必要 | v1.1 |
| ベンチマーク | 公開ベンチマークの出典・確認日が一部未整備 | 実データとして扱うには追加確認が必要 | v1.1 |
| CANAE評価 | CANAE実務評価フローは定義段階 | 評価の承認・更新履歴管理が必要 | v1.1 |
| 自動化 | 収集元選択、2ソース限定の読み取り専用外部取得、GitHub Changelog候補生成レポート、重複・差分検知レポート、pending候補登録、手動レビュー記録、Verified昇格CLIは実装済み。Review UIは未実装 | 候補確認と昇格操作はCLI中心の手動運用 | v1.1以降 |
| 運用 | データ更新レビューの担当・頻度は運用開始後に調整 | 更新責任の明確化が必要 | v1.1 |
| 表示 | ランキングスコアはv1.0サンプル値を含む | 実データ化済みメタ情報と混同しない表示が必要 | v1.1 |

## 不具合ではない項目

以下は現時点の仕様または運用方針です。

- 外部APIなし
- DBなし
- ログインなし
- `noindex`維持
- サンプル値はサンプル値として表示
- Verified以外のニュースは主要画面に表示しない
- 公開ベンチマークとCANAE評価は分離表示する

## v1.1 対応方針

v1.1では、機能追加よりもデータ品質と運用強化を優先します。

1. `tools.json` / `news.json`の実データ化
2. 公開ベンチマークの出典・確認日整備
3. CANAE評価の検証者、検証日、評価理由の整備
4. データ更新レビューの運用開始
5. Review UI、通知、昇格後の正本文書反映フローの段階実装

実データ投入前のスキーマは`14_v1.1_データモデル確定.md`を正とします。

## Freeze中の扱い

v1.0.1相当の凍結中は、この文書に記載された既知課題をPhase 1へ戻して実装しません。対応はv1.1以降で行い、Phase 1側では重大不具合や運用維持に必要な最小修正のみを扱います。

## 完了条件

この文書に記載された主要課題は、対応完了後に`11_v1.1_開発ロードマップ.md`、`12_リリース一覧.md`、`changelog/CHANGELOG.md`へ反映します。
